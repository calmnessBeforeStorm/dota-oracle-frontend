# Backend REST base. Dev: vite proxies /api to localhost:8100, so this can stay empty.
VITE_API_BASE_URL=
# WebSocket base for live updates (F5). Empty = derive from the current origin.
VITE_WS_BASE_URL=
